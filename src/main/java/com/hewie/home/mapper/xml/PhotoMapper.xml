<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hewie.home.mapper.PhotoMapper">

    <select id="listPhoto" resultType="com.hewie.home.entity.Photo">
        select * from t_photo limit #{offset}, #{size}
    </select>
</mapper>
